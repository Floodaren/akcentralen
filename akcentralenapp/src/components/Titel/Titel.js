import React, { Component } from 'react';

class Titel extends Component {

    render() {
        document.title = "Ak-Centralen jobbApp";
        return (
        <div>
        </div>
    );
  }
}

export default Titel;